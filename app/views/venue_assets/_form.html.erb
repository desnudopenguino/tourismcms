<%= form_for(@venue_asset) do |f| %>
  <% if @venue_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@venue_asset.errors.count, "error") %> prohibited this venue_asset from being saved:</h2>

      <ul>
      <% @venue_asset.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :venue_id %><br />
    <%= f.number_field :venue_id %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :asset %><br />
    <%= f.text_field :asset %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
