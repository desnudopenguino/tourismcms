<h1>Listing assets</h1>

<table>
  <tr>
    <th>Venue</th>
    <th>Type</th>
    <th>Location (URL)</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @assets.each do |asset| %>
  <tr>
    <td><%= asset.venue_id %></td>
    <td><%= asset.assetType %></td>
    <td><%= asset.location %></td>
    <td><%= link_to 'Show', venue_asset_path(asset.venue,asset) %></td>
    <td><%= link_to 'Edit', edit_venue_asset_path(asset.venue,asset) %></td>
    <td><%= link_to 'Delete', venue_asset_path(asset.venue,asset), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Asset', new_venue_asset_path %>
